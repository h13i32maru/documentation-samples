{
  "name": "for-firedoc",
  "version": "0.0.0",
  "scripts": {
    "build": "./node_modules/.bin/firedoc build ./src --dest ./doc --lint --lang en",
    "build-self": "./node_modules/.bin/firedoc build ./self/lib --dest ./doc-self --lint --lang en"
  },
  "dependencies": {
    "firedoc": "1.10.0"
  }
}
